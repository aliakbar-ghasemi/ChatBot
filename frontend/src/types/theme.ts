export interface Theme {
  colors: {
    primary: string;
    background: string;
    text: string;
    secondary: string;
    accent: string;
  };
  spacing: {
    small: string;
    medium: string;
    large: string;
  };
  typography: {
    h1: string;
    h2: string;
    body: string;
  };
}
